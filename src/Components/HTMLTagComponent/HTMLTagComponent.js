import React, { Component } from 'react';

class HTMLTagComponent extends Component {
  render() {
    return (
      <div className="HTMLTagComponent">
        HTML
      </div>
    );
  }
}

export default HTMLTagComponent;
